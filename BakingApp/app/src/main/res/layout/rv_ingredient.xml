<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/border"
    android:padding="8dp"
    android:layout_margin="4dp">
    <TextView
        android:id="@+id/tv_ingredient_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:text="This is ingredient"
        android:textSize="16sp"
        android:textAllCaps="true"
        android:textColor="@color/colorPrimaryDark"/>
    <TextView
        android:id="@+id/tv_ingredient_quantity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="12"
        app:layout_constraintLeft_toLeftOf="@+id/tv_ingredient_name"
        app:layout_constraintTop_toBottomOf="@+id/tv_ingredient_name"
        android:layout_marginTop="4dp"
        android:textColor="@color/colorSecondaryText"/>
    <TextView
        android:id="@+id/tv_ingredient_measure"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Gram"
        android:layout_marginLeft="4dp"
        app:layout_constraintLeft_toRightOf="@+id/tv_ingredient_quantity"
        app:layout_constraintTop_toTopOf="@+id/tv_ingredient_quantity"
        android:textColor="@color/colorSecondaryText"/>
    <ImageView
        android:id="@+id/iv_tick"
        android:layout_width="20dp"
        android:src="@drawable/ic_tick_"
        android:layout_height="20dp"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:visibility="invisible"/>

</androidx.constraintlayout.widget.ConstraintLayout>

